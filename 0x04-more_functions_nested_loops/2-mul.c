#include "main.h"

/**
 * mul - multiplies two numbers.
 * @a: first number.
 * @b: second number.
 *
 * Return: the product of both number.
 */

int mul(int a, int b)
{
	int prod;

	prod = a * b;
	return (prod);
}
